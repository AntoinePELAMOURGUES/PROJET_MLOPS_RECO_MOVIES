apiVersion: v1
kind: PersistentVolume
metadata:
  name: airflow-local-raw-preprocess-folder
spec:
  storageClassName: local-path
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  claimRef:
    namespace: airflow
    name: airflow-local-raw-preprocess-folder
  hostPath:
    path: "/host/data/src/raw/silver/" # Vérifiez ce chemin
