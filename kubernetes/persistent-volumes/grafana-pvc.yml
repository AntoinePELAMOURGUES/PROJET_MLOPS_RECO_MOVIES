apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-pvc
  namespace: api
spec:
  accessModes:
    - ReadWriteOnce # Mode d'accès
  resources:
    requests:
      storage: 1Gi # Taille demandée
